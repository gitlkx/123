若用一个大小为6的数组来实现循环队列，且当前rear和fornt的值分别为0和3。从当前队列中删除一个元素，
再加入两个元素后，rear和front的值分别为（ ）。
2和4

设一数列的顺序为1，2，3，4，5，6，通过队列操作可以得到（ ）的输出序列。
 1,2,3,4,5,6
 
 在一个顺序存储的循环队列中，若队尾指针指向队尾元素的后一个位置，则队
 头指针一般指向队头元素的（ ）。
 当前位置
 
 在由n个元素组成的顺序存储的循环队列sq中，假定f和r分别为队头指针和队尾指针，
 则判断队满的条件是（ ）。
 f ==（r+1）%n
 
 
 判断一个循环队列QU（最多元素为MaxSize）为空的条件是（）
 QU.front == QU.rear
